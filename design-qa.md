# 主界面设计验收

## 对比输入

- 设计参考：`docs/evidence/R1-MAIN-UI-REDESIGN-OPTION-1.png`（原始比例约 16:9）。
- 最终实机构建：`docs/evidence/R1-MAIN-UI-REDESIGN-V2-1280x720.png`。
- 同屏设计对比：`docs/evidence/R1-MAIN-UI-DESIGN-COMPARISON-V2.png`，左侧为参考，右侧为实现。
- 按钮状态对比：`docs/evidence/R1-MAIN-UI-BUTTON-COMPARISON-V2.png`，从上到下依次为普通、悬浮、点击选中状态。
- 验收视口：1280 x 720，Web Desktop。

## 本轮迭代记录

- P1：旧 HUD 背景已经绘制六个导航槽位和橙色战斗槽位，又叠加独立 Button，造成双层边框和重复背景；已改为无按钮槽位的连续金属安装轨，按钮完全独立。
- P1：旧按钮状态平、轮廓不统一；已生成并导入深蓝导航与橙色主行动按钮素材，普通、悬浮、按下三态均使用同一立体轮廓派生，避免状态切换时尺寸和外形跳变。
- P2：导航文字垂直居中且缺少图形层级；已补齐六枚独立图标，图标位于上半区，标签移至下半区，并调整顶栏与右侧状态文字位置、字号和描边。
- P1：第一次悬浮检查发现生成图残留棋盘格白边；已从普通态的真实 Alpha 和同轮廓重新派生悬浮、按下态，刷新 Cocos AssetDB 后复测，白边已消失。

## 组件与素材检查

- 主界面框架使用独立的 1280 x 720 HUD Sprite，只提供顶框、右侧信息窗和连续底部安装轨，不承载按钮图形或点击区。
- 六个底部导航按钮各自使用 142 x 82 的独立节点；素材源为 284 x 164 的 `nav-v2-normal.png`、`nav-v2-hover.png`、`nav-v2-pressed.png`。
- 六枚导航图标是独立 96 x 96 PNG，并作为按钮子节点绑定，不烘焙在背景或按钮底板中。
- “进入战斗”按钮使用独立的 320 x 100 节点；素材源为 640 x 200 的 `battle-v2-normal.png`、`battle-v2-hover.png`、`battle-v2-pressed.png`。
- 普通、悬浮、按下状态通过 Cocos `Button.Transition.SPRITE` 原生切换。
- 右侧状态面板保留透明内容窗口，实时能源、房间、船员、维修和治疗信息没有被静态框架遮挡。

## 交互与运行检查

- 普通态：深蓝金属按钮与底部安装轨分层清楚，没有重复槽位。
- 悬浮态：鼠标移入“星图”后切换为更亮的青色 SpriteFrame，轮廓、图标和文字位置稳定，无白边。
- 点击态：点击“星图”后路由切页，顶栏标题与选中按钮同步变化。
- 主行动态：鼠标移入“进入战斗”后橙色面板增强发光，仍保持完整金属外框。
- 恢复态：返回“主界面”后页面内容与按钮选中状态恢复。
- 浏览器 Console：0 条 warning，0 条 error。

## 视觉结论

- P0：无。
- P1：无。
- P2：无。
- P3：无阻断项。
- 同屏对比确认：深色舰体材质、青色信息层级、独立立体导航按钮、图标在上文字在下，以及橙色主行动按钮均与参考方向一致；实际玩法内容和实时状态信息保持可读。

final result: passed
